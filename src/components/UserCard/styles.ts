import { createStyles } from '@mui/styles';

export const styles = createStyles({
    userCard: {
        display: 'flex',
        alignItems: 'center'
    },
    userAvatar: {
        margin: '8px'
    },
    userDetailsSection: {
        margin: '2px 8px',
        flexGrow: 1
    },
    skeleton: {
        width: '95%'
    }
});
